{
    "terminal.integrated.env.windows": {
        "NODE_OPTIONS": "--dns-result-order=ipv4first --network-family-autoselection-attempt-timeout=1000"
    },
    "terminal.integrated.env.linux": {
        "NODE_OPTIONS": "--dns-result-order=ipv4first --network-family-autoselection-attempt-timeout=1000"
    },
    "terminal.integrated.env.osx": {
        "NODE_OPTIONS": "--dns-result-order=ipv4first --network-family-autoselection-attempt-timeout=1000"
    }
}